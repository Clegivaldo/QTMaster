# Frontend Environment Variables for Docker
# Use Vite env names so the build/runtime uses the same base (`/api`) and
# nginx can proxy `/api` to the backend service inside compose.
VITE_API_URL=/api
VITE_TEMPLATE_EDITOR_URL=/api/template-editor
VITE_APP_ENVIRONMENT=production
VITE_REPORT_GENERATION_TIMEOUT=120000