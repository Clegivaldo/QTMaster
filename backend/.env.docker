# Database (match docker-compose .env defaults)
# Use the same defaults as the root .env so containers started by compose have consistent credentials
DATABASE_URL=postgresql://laudo_user:laudo_password@postgres:5432/laudo_db

# Redis
REDIS_URL=redis://redis:6379

# Server
PORT=3000
NODE_ENV=production

# File Upload
MAX_FILE_SIZE=52428800
UPLOAD_DIR=./uploads

# PDF Generation
PDF_OUTPUT_DIR=./generated_pdfs
PUPPETEER_HEADLESS=true

# JWT
JWT_SECRET=5a0bbeea92885bb0dc4a366699cfab9b02384203f019c8aa
JWT_EXPIRES_IN=1d

# CORS and frontend
CORS_ORIGIN=http://10.11.12.233:8081
FRONTEND_URL=http://10.11.12.233:8081