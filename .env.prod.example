# Database Configuration
POSTGRES_DB=laudo_db
POSTGRES_USER=laudo_user
POSTGRES_PASSWORD=your-super-secure-postgres-password

# Redis Configuration
REDIS_PASSWORD=your-super-secure-redis-password

# JWT Configuration
JWT_SECRET=your-super-secure-jwt-secret-key-at-least-32-characters
JWT_REFRESH_SECRET=your-super-secure-jwt-refresh-secret-key-at-least-32-characters
JWT_EXPIRES_IN=8h
JWT_REFRESH_EXPIRES_IN=7d

# Application Configuration
FRONTEND_URL=https://yourdomain.com
VITE_API_URL=/api
VITE_APP_NAME=Sistema de Laudos Térmicos

# FastReport Configuration
FASTREPORT_LICENSE=your-fastreport-license-key

# Backup Configuration
BACKUP_ENCRYPTION_KEY=your-backup-encryption-key-32-chars

# Monitoring Configuration
GRAFANA_PASSWORD=your-grafana-admin-password

# Logging Configuration
LOG_LEVEL=info

# SSL Configuration (if using HTTPS)
SSL_CERT_PATH=/etc/nginx/ssl/cert.pem
SSL_KEY_PATH=/etc/nginx/ssl/private.key

# Email Configuration (for notifications)
SMTP_HOST=smtp.yourdomain.com
SMTP_PORT=587
SMTP_USER=noreply@yourdomain.com
SMTP_PASSWORD=your-smtp-password
SMTP_FROM=Sistema de Laudos <noreply@yourdomain.com>

# Backup Storage Configuration
BACKUP_S3_BUCKET=your-backup-bucket
BACKUP_S3_REGION=us-east-1
BACKUP_S3_ACCESS_KEY=your-s3-access-key
BACKUP_S3_SECRET_KEY=your-s3-secret-key

# git add .;git commit -m "Melhorias";git push
# docker-compose build --no-cache
# docker-compose up -d --force-recreate




# 1 - Ajuste para selecionar vários itens ao clicar segurando CTRL e clicar em cada um, um por um.
# 3 - Ajuste para mover a página ao clicar e arrastar dentro dela(com ícone da mão).
# 4 - Ajuste para quando clicar na página... fora dos itens, ou fora da página, remover a seleção do ítem selecionado.
# 5 - Desfazer, CTRL+Z não está funcionado. Ajuste para funcionar.
# 6 - Ajuste para opção de adicionar e remover página (Ainda está com apenas uma página mesmo adicionando).
# 7 - Ajuste para ter opção de separar uma parte da página para ser cabeçalho e rodapé. Podendo ou não ser adicionado e podendo ou não ser replicado para várias páginas no mesmo layout, com opção de ajustar tamanho (altura).
# 8 - Ajuste botão da Galeria, para ficar igual os outros botões (salvar, abrir, visualizar), e dentro da galeria, botão upload apenas circulo com ícone e em cada imagem botão deletar.